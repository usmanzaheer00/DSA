#include<iostream>
using namespace std;

class DNode
{
	public:
		double data;
		DNode* next;
		DNode* prev;
		
		DNode(double i =0 , DNode *n=0 , DNode *p=0)      // Constructor
		{
			data = i;
			next = n;
			prev = p;
		}
};

